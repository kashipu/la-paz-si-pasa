; ponytail: mu-plugins are bind-mounted and edited outside of a container restart.
; Force OPcache to re-stat the file on every request so code changes (and any
; drift after a redeploy) take effect without a manual container restart.
opcache.revalidate_freq=0
